# Bitcoin-Transaction-Signature-Extractor-Analyzer
> ⚙️ **Advanced Bitcoin transaction decoder and ECDSA signature extractor**   > This tool fetches raw Bitcoin transactions from multiple APIs,   > decodes input scripts, extracts public keys and signatures,   > reconstructs signing preimages, and computes the `z` hash used in ECDSA verification.
